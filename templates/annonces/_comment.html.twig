<div class="card">
    <div class="header">
        <div class="row">
            <div class="col-lg-9 col-md-9 col-sm-9 col-xs-9">
                Par {{ comment.author.username }} le {{ comment.date | date ('d/m/Y') }}
                à {{ comment.date | date('H:i') }}.
            </div>
            <div class="col-lg-3 col-md-3 col-sm-3 col-xs-3">
                {% if is_granted("COMMENT_EDIT", comment) %}
                    <a href="{{ path("del_com",{id:comment.id}) }}" class="btn btn-block bg-pink">Supprimer</a>
                {% endif %}
            </div>
        </div>
    </div>
    <div class="body">
        {{ comment.content }}
    </div>
</div>